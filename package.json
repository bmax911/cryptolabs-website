{
  "name": "cryptolabs-portfolio-website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "copy-html": "cp -r templates/* dist/",
    "copy-static": "cp -r static dist/",
    "build": "npm run clean && mkdir dist && npm run copy-html && npm run copy-static"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "netlify-cli": "^22.2.1",
    "node-fetch": "^2.7.0"
  }
}
